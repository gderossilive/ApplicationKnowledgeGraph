--- a/pom.xml
+++ b/pom.xml
@@ -18,3 +18,3 @@
-		    <groupId>com.microsoft.sqlserver</groupId>
-		    <artifactId>mssql-jdbc</artifactId>
-		    <version>7.2.2.jre8</version>
+		    <groupId>org.postgresql</groupId>
+		    <artifactId>postgresql</artifactId>
+		    <version>42.2.27</version>
--- a/src/main/java/com/microsoft/aad/oidcpoc/db.java
+++ b/src/main/java/com/microsoft/aad/oidcpoc/db.java
@@ -25,1 +25,1 @@
-    String url = String.format("jdbc:sqlserver://%s:1433;database=%s;user=%s;password=%s;encrypt=true;hostNameInCertificate=*.database.windows.net;loginTimeout=30;", hostName, dbName, user, password);
+    String url = String.format("jdbc:postgresql://%s:5432/%s?sslmode=disable", hostName, dbName);
@@ -67,4 +67,4 @@
-        url = String.format("jdbc:sqlserver://%s:1433;database=%s;user=%s;password=%s;encrypt=true;hostNameInCertificate=*.database.windows.net;loginTimeout=30;", hostName, dbName, user, password);
+        url = String.format("jdbc:postgresql://%s:5432/%s?sslmode=disable", hostName, dbName);
         try {
-			connection = DriverManager.getConnection(url);
-            logger.info("Connecting DB via sql username and password");
+			connection = DriverManager.getConnection(url, user, password);
+            logger.info("Connecting DB via PostgreSQL username and password");
@@ -97,1 +97,1 @@
-		String updsql = "UPDATE [User] SET UniqueId = ?, Password = ? WHERE UserId = ?";
+		String updsql = "UPDATE \"User\" SET \"UniqueId\" = ?, \"Password\" = ? WHERE \"UserId\" = ?";
@@ -117,1 +117,1 @@
-            String selectSql = "SELECT * FROM [User] WHERE UniqueId = ?";
+            String selectSql = "SELECT * FROM \"User\" WHERE \"UniqueId\" = ?";
@@ -146,1 +146,1 @@
-            String selectSql = "SELECT * FROM [User] WHERE Email = ? AND Password = ?";
+            String selectSql = "SELECT * FROM \"User\" WHERE \"Email\" = ? AND \"Password\" = ?";
--- /dev/null
+++ b/dbschema.postgresql.sql
@@ -0,0 +1,14 @@
+CREATE TABLE IF NOT EXISTS "User" (
+    "UserId" integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
+    "Email" varchar(100) NOT NULL,
+    "UniqueId" varchar(100),
+    "FName" varchar(50),
+    "LName" varchar(50),
+    "Password" varchar(20)
+);
+
+CREATE TABLE IF NOT EXISTS "Thing" (
+    "ThingId" integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
+    "UserId" integer NOT NULL REFERENCES "User"("UserId") ON DELETE CASCADE,
+    "Thing" varchar(50)
+);
